# Read by vite.config.ts only (never bundled into the app). Used by both
# `npm run dev` and `npm run preview` to proxy /api and /ws to the backend.
VITE_BACKEND_URL="http://localhost:3001"
VITE_PORT="5173"
